# Node / pnpm
node_modules/
.pnpm-store/
*.tsbuildinfo

# build / test output
dist/
build/
coverage/
.vitest-cache/

# env
.env
.env.*

# editors
.DS_Store
.vscode/
.idea/

# local task 产物（LocalTracker 用）
tasks/
logs/
typescript/.codebuddy-auto/

# spike / probe 原始数据（含账号响应，本地保留）
tmp/

# codebuddy：只保留项目级 rules，其他运行时/个人数据忽略
.codebuddy/*
!.codebuddy/rules/
!.codebuddy/rules/**

# Claude Code worktrees (created by EnterWorktree). Ignore the whole tree;
# it's a per-session staging area, not part of the source repository.
.claude/worktrees/

# Claude Code worktrees (created by EnterWorktree). Ignore the whole tree;
# it's a per-session staging area, not part of the source repository.
.claude/worktrees/

# Project-local git worktrees used for isolated feature implementation.
.worktrees/
