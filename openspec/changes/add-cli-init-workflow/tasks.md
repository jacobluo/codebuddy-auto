## 1. CLI Init Contract

- [x] 1.1 Add failing CLI tests for `codebuddy-auto init` generation, workspace creation, and overwrite protection.
- [x] 1.2 Implement the `init` subcommand and filesystem writes.

## 2. Documentation

- [x] 2.1 Update README to present the init-first runtime directory flow.
- [x] 2.2 Update example workflow guidance if needed to distinguish examples from initialized runtime files.

## 3. Verification

- [x] 3.1 Run focused CLI tests.
- [x] 3.2 Run `pnpm run build`, `pnpm run check`, and `pnpm run test`.
